## 1. 依赖、配置与数据契约

- [ ] 1.1 安装并锁定官方 `wechatpay/wechatpay` SDK，确认 PHP/Guzzle 兼容性并执行 Composer 安全审计
- [ ] 1.2 先编写微信配置预检测试，再实现 AppID、商户号、证书序列号、公钥 ID、密钥路径、API v3 Key、通知 URL、H5 网站信息和订单 TTL 配置
- [ ] 1.3 更新 `.env.example` 与生产环境保护规则，使非 local/testing 环境不能使用默认 Mock Secret 或不完整微信配置
- [ ] 1.4 先编写迁移 Smoke 测试，再为支付单增加 `expires_at`、`last_queried_at`、`gateway_error_code`、`gateway_error_at` 和补偿扫描复合索引
- [ ] 1.5 先更新枚举与状态迁移测试，再增加支付单 `creating` 状态及 `creating|pending -> paid` 等合法迁移规则
- [ ] 1.6 定义渠道无关的 Checkout、查单结果和已验证支付通知 DTO，并用单元测试冻结金额、币种、订单号和时间字段契约

## 2. 渠道化 Checkout 与微信 H5 网关

- [ ] 2.1 先扩展 Mock 行为测试，再扩展 `PaymentGateway` 的 H5 下单、按商户订单号查单和关单能力
- [ ] 2.2 先编写十进制元转整数分测试，再实现不经过浮点的 CNY 金额转换与边界校验
- [ ] 2.3 使用 fake 传输层先编写微信 H5 下单测试，覆盖请求签名输入、必需字段、客户端 IP、H5 场景、超时、业务错误和响应映射
- [ ] 2.4 实现隔离官方 SDK 的 `WeChatPayGateway`，完成 H5 下单、查单、关单及响应验签错误分类
- [ ] 2.5 重构依赖绑定，使默认配置只决定新订单渠道，而 Mock 与微信通知/补偿能力始终可解析
- [ ] 2.6 先编写 Checkout Feature 测试，再将本地订单创建与远程微信下单拆为短事务两阶段流程，并保持现有 `checkout_url` 响应兼容
- [ ] 2.7 覆盖下单结果不确定时复用原商户订单号查单、不得创建重复交易的回归测试

## 3. 微信通知验签、解密与接收

- [ ] 3.1 建立仅供测试的 RSA 密钥和 API v3 Key 夹具，提供微信格式签名和 AES-256-GCM 通知生成器
- [ ] 3.2 先编写通知验签测试，覆盖合法签名、正文篡改、错误/未知公钥 ID、过期时间戳和 `WECHATPAY/SIGNTEST/` 探测流量
- [ ] 3.3 先编写认证解密测试，覆盖合法资源、错误 GCM 标签、错误 nonce/associated data、非预期算法和畸形 JSON
- [ ] 3.4 实现微信通知验证与解密组件，输出渠道无关通知 DTO，且异常和日志不包含密钥或完整密文
- [ ] 3.5 先编写一致性拒绝测试，再实现 AppID、商户号、`MWEB`、`SUCCESS`、订单渠道、CNY 和整数分金额校验
- [ ] 3.6 提取 Mock 与微信共用的支付事件落库服务，保留 `(provider, event_id)` 幂等及相同 ID 不同载荷冲突检测
- [ ] 3.7 先编写微信回调 Feature 测试，再实现 `/api/v1/payments/webhooks/wechat` 的空体 204、校验失败 4xx 和持久化失败 5xx 响应

## 4. 幂等业务处理与补偿

- [ ] 4.1 扩展支付处理测试，验证微信成功事件原子更新事件、订单、订阅和租户套餐，并保存唯一微信交易号
- [ ] 4.2 调整业务处理器接受标准通知载荷，支持 `creating`/`pending` 支付成功且重复 Job 不重复激活
- [ ] 4.3 先编写确定性补偿事件 ID 测试，再实现查单 `SUCCESS` 通过同一事件处理链路补偿入账
- [ ] 4.4 先编写到期扫描测试，再实现按 `(provider, status, expires_at)` 小批次派发单订单补偿 Job
- [ ] 4.5 实现 `NOTPAY` 订单先微信关单后本地取消，并测试网络/可重试 5xx 时保留非终态和退避重试
- [ ] 4.6 注册 Scheduler，增加分布式互斥或等效并发保护，并测试任务注册、重复扫描和并发执行幂等性
- [ ] 4.7 为最终失败事件、失败队列、回调延迟和超时订单补充结构化脱敏日志及监控指标

## 5. 契约、文档与部署验收

- [ ] 5.1 更新 OpenAPI，记录订阅 Checkout 的微信 `checkout_url` 语义和微信回调端点的专用响应协议
- [ ] 5.2 更新架构与运维文档，说明 Secret 文件挂载、配置预检、Worker/Scheduler、失败队列、告警和公钥轮换流程
- [ ] 5.3 运行微信支付专项单元/Feature 测试、现有 Mock 订阅支付回归测试、迁移 Smoke 测试和全量测试，记录所有环境性失败
- [ ] 5.4 运行代码格式化、静态/语法检查、OpenSpec 严格校验和依赖安全审计，并确认日志/仓库无真实密钥
- [ ] 5.5 在上线环境执行迁移预演、配置预检、回调公网连通性检查及 Worker/Scheduler 健康检查
- [ ] 5.6 使用真实小额订单验收 H5 拉起、支付、回调激活、重复通知、主动查单补偿和未支付关单，并形成可回滚验收记录
